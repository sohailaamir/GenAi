
version: "3.9"
services:
  app:
    build: .
    image: genai-router:latest
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      STREAMLIT_BROWSER_GATHER_USAGE_STATS: "false"
